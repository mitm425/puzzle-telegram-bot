services:
  - type: web
    plan: free
    name: mystery-roles-bot
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        value: ваш_токен_здесь
    healthCheckPath: /health
